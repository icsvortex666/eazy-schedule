import React, { FC } from 'react';

const Navbar: FC = () => (
  <nav className="navbar navbar-dark bg-dark p-0">
    <a
      className="navbar-brand font-weight-bold mx-auto"
      href="/"
    >
      EazySchedule
    </a>
  </nav>
);

export default Navbar;
